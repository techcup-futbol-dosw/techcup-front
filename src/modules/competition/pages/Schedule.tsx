import { PageShell } from "@/core/components/PageShell";

export function Schedule() {
  return (
    <PageShell
      eyebrow="Competencia"
      title="Calendario"
      description="Página base para el calendario del torneo y sus jornadas."
    />
  );
}