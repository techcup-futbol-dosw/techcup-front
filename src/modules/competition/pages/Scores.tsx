import { PageShell } from "@/core/components/PageShell";

export function Scores() {
  return (
    <PageShell
      eyebrow="Competencia"
      title="Tabla de posiciones"
      description="Página base para el ranking y las estadísticas del campeonato."
    />
  );
}