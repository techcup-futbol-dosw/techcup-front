import { PageShell } from "@/core/components/PageShell";

export function Dashboard() {
  return (
    <PageShell
      eyebrow="Competencia"
      title="Dashboard"
      description="Vista base para el módulo de competencia. La navegación ya queda habilitada para el resto de la app."
    />
  );
}