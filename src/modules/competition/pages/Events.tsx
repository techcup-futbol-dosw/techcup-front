import { PageShell } from "@/core/components/PageShell";

export function Events() {
  return (
    <PageShell
      eyebrow="Competencia"
      title="Eventos"
      description="Página placeholder para listar eventos, incidencias y actividad deportiva del torneo."
    />
  );
}