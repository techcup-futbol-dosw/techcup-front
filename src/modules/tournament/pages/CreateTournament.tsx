import { PageShell } from "@/core/components/PageShell";

export function CreateTournament() {
  return (
    <PageShell
      eyebrow="Organizador"
      title="Crear torneo"
      description="Formulario base para iniciar la creación de un nuevo torneo."
    />
  );
}