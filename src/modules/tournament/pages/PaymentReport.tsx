import { PageShell } from "@/core/components/PageShell";

export function PaymentReport() {
  return (
    <PageShell
      eyebrow="Organizador"
      title="Reporte de pagos"
      description="Pantalla base para consultar pagos, estado de cuotas y validaciones."
    />
  );
}