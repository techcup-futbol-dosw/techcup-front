import { PageShell } from "@/core/components/PageShell";

export function ManageTournaments() {
  return (
    <PageShell
      eyebrow="Organizador"
      title="Gestionar torneos"
      description="Listado base para administrar torneos existentes."
    />
  );
}